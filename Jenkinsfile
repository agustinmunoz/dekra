pipeline {
            agent any
            
            stages {
                stage('preamble') {
                    steps {
                    echo 'Hola pruebas de OpenShift'
                    }
                       
                } // stage
            } // stages
        } // pipeline
